[project]
name = "afc-sidecar"
version = "0.1.0"
description = "AFC Sidecar — Local device agent for endpoint governance"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "httpx>=0.28.0",
    "pyyaml>=6.0.2",
]
